<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Sumte" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Sumte" parent="Base.Theme.Sumte">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Signup.Sumte" parent="Theme.AppCompat.Light.NoActionBar">

    </style>



    <style name="Theme.RangeSlider" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    </style>

<!--    이미지 크게 보여주는 다이얼로그 뒷배경(투명)-->
    <style name="Theme.TransparentDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>